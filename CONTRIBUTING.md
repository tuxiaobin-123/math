# 为 CUMCM Lens 贡献

本项目接受赛题数据字典、可复现模型、测试、训练卡和纠错。不要提交付费材料、未经授权的优秀论文全文、官方原始压缩包或无法说明来源的数字。

## 最小贡献单元

1. 选择一个明确问题，不同时扩展多个题目。
2. 为事实加入来源 ID；为计算结果加入脚本、环境、随机种子和输出校验值。
3. 运行 `python -m pytest` 与 `python -m compileall -q src scripts tests`。
4. 对时序任务使用时间外推或滚动验证；对优化任务报告求解状态和 gap。
5. 在 PR 中填写“事实 / 可复算结果 / 建议”三类声明。

## 复现徽章

- `source-checked`：来源、文件名和校验值齐全。
- `code-runs`：干净环境可执行。
- `result-reproduced`：关键指标在声明容差内重现。
- `independent-review`：由非作者复跑并记录环境。

徽章代表仓库内部审计状态，不代表 CUMCM 官方认证。
